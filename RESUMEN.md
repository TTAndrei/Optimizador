# RESUMEN — Optimizador CFD 2D

## Qué es
Simulador CFD 2D incompresible (Navier-Stokes) en GPU (CuPy, RTX 3070 Ti) para perfiles alares, base de un futuro optimizador aerodinámico. Todo el solver vive en `Simulador2D.py` (~8000 líneas, clase `Mesh` + `main()`).

- Malla cartesiana estirada (zona fina alrededor del perfil), advección semi-Lagrangiana, difusión (+WALE LES opcional), proyección de presión multigrid/CG.
- Sólidos por IBM (Immersed Boundary): máscara rasterizada + ghost-cell no-slip (`ibm_wall_mode="ghost_noslip"`).
- **Ejecutar SIEMPRE con `.venv/bin/python`** (el python3 del sistema no tiene CuPy).

## Estado actual (2026-07-12, rama Malla-Variable)

Caso de trabajo: NACA0012, α=5°, Re=1e5, Cl físico esperado ~0.55.

**FIX DE RAÍZ IMPLEMENTADO: `wall_treatment="consistent"`** (kwarg de `main()`, default "legacy" intacto). El sumidero IBM está eliminado. Componentes (todas reutilizan piezas existentes):
1. Divergencia por caras en la proyección (`divergence_form="face_flux"` → `_compute_flux_divergence_field_uv`): cara fluido-sólido = flujo 0 exacto → el Poisson ve el flujo contra la pared.
2. Gradiente de presión one-sided en celdas de pared (`wall_pressure_gradient_mode="one_sided"`, kernel ya existía).
3. `reforzar_impermeabilidad` desactivado (`disable_reforzar=True`) — era la cirugía que borraba masa (Q≈-0.06·U·c).
El Laplaciano masked ya era Neumann correcto; el problema era que divergencia y corrección anulaban la componente entera junto al sólido (proyección ciega en pared).

**Resultados validados** (JSON+npz en `results/agent_tests/wc_*`; commiteados en `a5c14b7`, npz >100MB excluidos por `.gitignore`):
- `wc_re1e3_a5` (Re=1e3, α=5): Cl=0.211±0.0004, Cd=0.148, Q_lazo=+0.02, ΔCp_TE=0.04. **Clava el DNS de referencia (Kurtulus Re=1000: Cl~0.22, Cd~0.146)**. Legacy daba 0.14 con Q=-0.06.
- `wc_sa_a0` (α=0, SA): Cl=0.0003, sin NaN — simetría perfecta.
- `wc_sa_a5_dx2` (α=5, Re=1e5, SA, dx=0.002, turbo_hd): **estable sin colapso**, Kutta cierra (ΔCp_TE=0.012), pero Cl=0.274. Q_lazo=+0.026 (MG turbo saturado).
- `wc_sa_a5_dx2_full` (sin turbo: div=0.02, outer=8, niveles=2, 24k iters): Q_lazo=0.006 ✓ pero Cl=0.312. **Hipótesis Q→Cl pendiente ~20 FALSIFICADA**: ΔQ=0.020 predecía ΔCl≈+0.4, medido +0.04.
- `wc_sa_a5_dx2_long` (div=0.01, outer=12, 48k iters, t=18.3 conv, 2h GPU): Q=0.0032, **Cl CONVERGE en plateau 0.32** (transitorio: pico 0.40 en t≈2, mínimo 0.312 en t≈10, luego estabiliza 0.320 con deriva 0.0025). cl_cp=0.383, cl_circ=0.281 (dispersión estimadores cerrando). x_succión mejoró a 0.045. **PROYECCIÓN EXONERADA: Q→0 no recupera el Cl.**
- **Diagnóstico del déficit (0.32 vs 0.55)**: SA sobre-difunde. Evidencia: BL en x/c=0.5 espesor ~0.07c (3× placa plana turbulenta Re=1e5), χ=nu_t/nu~40-50 en BL (teórico ~8), Cp_min=-0.81 (esperado ~-1.9), Cd=0.048 (alto). Causas candidatas: (a) SA fully-turbulent desde LE a Re=1e5 (flujo real laminar hasta ~x/c=0.5, penaliza succión), (b) primera celda a y+≈10 sin wall function (SA quiere y+≲1). Distancia de pared SA verificada correcta; constantes SA estándar.
- Hallazgo menor: `mg_modo_turbo_hd` pisaba `divergencia` (arreglado: `min(divergencia, 0.05)`); runs `wc_sa_a5_dx2` y `_t02` idénticos (el fix no cambió nada porque turbo satura outers igual).
- Añadido kwarg `sa_nu_tilde_factor` a `main()` (default 3.0; bajo ~0.1 retrasa transición SA).
- `wc_sa_a5_dx2_lam01` (sa_nu_tilde_factor=0.1, t=14.2 conv): **PEOR** — Cl=0.266, x_succión=0.14 (LSB reapareciendo), Cp_min=-0.66, Cd=0.056; χ_max=184 (transiciona igual pero tarde). **Falsificada hipótesis (a) fully-turbulent**; retrasar transición reactiva la burbuja laminar. Mantener sa_nu_tilde_factor=3.0.
- **Sospecha cuantificada: difusión numérica del semi-Lagrangiano bilineal.** nu_num≈α(1-α)·dx²/(2dt)~1e-4 cerca de pared = 10× nu molecular a Re=1e5 (a Re=1e3 era ~10% de nu → por eso el DNS clavó). Escala ∝dx (dt∝dx por CFL). Junto con y+≈10, motiva test de resolución.
- `wc_sa_a5_dx1` (dx=0.001, 60k iters, t=11 conv, 3.6h GPU): **Cl=0.389** (vs 0.32 a dx=0.002), Cd=0.039, Cp_min=-0.96, χ_max=105, estable. **Camino resolución confirmado, error ~1º orden en dx.** Extrapolando dx→0: Cl≈0.46. dx=0.0005 no cabe en la 3070 Ti.
- **Causa dominante identificada: difusión numérica del SL bilineal, nu_num≈u·dx/2 (independiente de dt)** — 1º orden, ~5-10× nu molecular cerca de pared a Re=1e5. A Re=1e3 era ~10% de nu (por eso clavó DNS).
- **MacCormack implementado** (`advection_scheme="maccormack"` en main(), default "sl"): corrección Selle 2008 (trace forward + φ*+(φ-φ**)/2) con limitador min/max del stencil y **banda de 2 celdas junto al sólido en SL puro** (sin banda, la corrección sobre el staircase mete pico espurio Cp=-11 en TE; con banda, α=0 perfecto Cp_min=-0.47). Validación Re=1e3 α=5: Cl=0.245/Cd=0.155 (SL: 0.211/0.148, DNS: 0.22/0.146) — ligero overshoot coherente con menos difusión en malla coarse, física intacta.
- `wc_mc_sa_a5_dx2` (dx=0.002, SA, MacCormack, t=11.5 conv): **Cl=0.452** (SL daba 0.32), Cp_min=-1.20, x_succión=0.015 (posición física del LE ✓), χ_max=73, Cd=0.040, std(Cl)=1e-5 (estacionario casi a máquina, deciles planos desde t≈4, sin sangrado). **Cuarta capa (nu_num del SL) CONFIRMADA y resuelta.** Coste MacCormack: ~4% vs SL.
- `wc_mc_sa_a5_dx1` (dx=0.001 + MacCormack, t=9.7 conv, 3.6h): **Cl=0.496 ✓ OBJETIVO CUMPLIDO** (criterio [0.4,0.7]), Cd=0.0335 (físico ~0.03 ✓), Cp_min=-1.24, x_succión=0.02 ✓, Q=0.005 ✓, deriva 0.001. **Richardson dx→0: Cl≈0.54 — clava el físico 0.55.** Convergencia de malla limpia (0.452@dx2 → 0.496@dx1).
- **CONFIG DE REFERENCIA DEL OPTIMIZADOR**: `wall_treatment="consistent"` + `turb_model="sa"` + `advection_scheme="maccormack"`. dx=0.002 para screening (Cl-0.1 vs físico, ~65 min/run), dx=0.001 para validación (Cl-0.05, ~3.6h).
- Regresión verde tras MacCormack (5 passed; defaults intactos, "sl" sigue siendo default).
- **POLAR VALIDADA** (`wc_mc_polar_a0/a2/a8` + `wc_mc_sa_a5_dx2`, dx=0.002 MacCormack): Cl(α)= 0(0.000), 2(0.185), 5(0.452), 8(0.708); Cd= 0.0305/0.0323/0.0404/0.0624. Monotonía ✓, pendiente 0.088/deg ✓, simetría α=0 exacta ✓, sublinealidad física en α=8 (pre-stall), Cp_min=-2.13 y succión x/c=0.015 en α=8. **DIAGNÓSTICO DE 4 CAPAS CERRADO COMPLETO.**
- **Todo commiteado** (`a5c14b7` "lift y drag solucionados": MacCormack + sa_nu_tilde_factor + resultados wc_*; `93b6c54`: script de barrido; `5979fab`: .gitignore para npz >100MB; `842fd9f`: última actualización de estado).
- **Script de barrido CFL/dx/α añadido**: `scripts/agent_tests/run_cfl_sweep.py` — cola resumible de 30 sims (α=0/2/5/8/10 × CFL=0.25/0.5/0.75 × dx=0.004/0.002) con la config de referencia, iteraciones auto-escaladas a t=12 convectivos, métricas JSON por sim (iteración/tiempo de convergencia de Cl/Cd), npz de estado, frames cada 1000 iters, streamlines finales, series temporales. Reanuda desde la última completada; CSV resumen + plots polar/convergencia/coste-vs-error. **Aún no ejecutado** (`results/cfl_sweep/` solo tiene `frames/` vacío; verificado 2026-07-03).
- **Feature de prueba de IC simplificada renombrada (sin commitear, working tree)**: `Mesh.__init__` y `main()` reemplazan `core_v0x/core_v0y/core_box` por `v0x_ic/v0y_ic` — la IC deja de ser una caja interior y pasa a ser una IC uniforme distinta a v0x/v0y en TODA la malla (fronteras/freestream siguen gobernadas por v0x/v0y). Simplificación del feature de prueba de transitorios, sin impacto en el comportamiento por defecto (None = sin efecto). El bloque `__main__` de referencia subió `iteraciones` de 6000 a 10000, cambió el perfil de prueba a AG24, activó `frames_dir_grueso="prueba_inicialesdiferentes"` y desactivó `stop_on_clcd_convergence` — configuración de scratch para explorar transitorios, no representa la config de referencia del optimizador (esa sigue siendo consistent+SA+MacCormack documentada arriba).
- **Scripts de barrido outer_sum actualizados (sin commitear)**: `scripts/comparativa_barrido_completo.py` y `scripts/script_barrido_alpha.py` separan explícitamente `Cl_final` (simulación directa) de `cl_cp_at_convergence` (integral de ∮ΔCp) como dos series independientes en plots/estadísticas, en vez de resolver ambigüedad vía `preferred_cl_column`. Se añadió modo `--smoke` a `comparativa_barrido_completo.py` (1 perfil, 1 Re, 3 alphas, malla gruesa dx=0.01, 50 iters, salida en `results/comparativas/smoke_test/`) para validar el pipeline sin gastar GPU. Defaults cambiados: `--alphas` de `-10:10:1` a `0:10:1`, `--iteraciones` de 2000 a 6000, `--dx-min` de 0.001 a 0.002. Esto regeneró localmente `results/comparativas/resultados_comparacion_outer_sum/*` (modificado, no commiteado) y dejó obsoletas/borradas las carpetas antiguas `results/comparativas/resultados_comparacion/{AG24,GM15,NACA0012}_{100k,1M}` (sin la distinción Cl_cp) — pendiente confirmar y commitear como reemplazo.

**Diagnóstico histórico cerrado (4 capas, evidencia en `results/agent_tests/`)**:
1. LSB laminar a Re≥1e4 (burst t≈5-7 convectivos, colapso). Fix: **Spalart-Allmaras** (`turb_model="sa"`). WALE inerte.
2. Pared sub-resuelta: SA necesita dx≤0.002 (y+≲10).
3. Inconsistencia IBM↔proyección: legacy absorbía masa (sobre-circulación 2.2×); compatible_flux desangraba vorticidad (Cl→0.1). **Resuelta con wall_treatment="consistent"**.
4. Difusión numérica del SL bilineal (nu_num≈u·dx/2, 1º orden): BL 3× gruesa, Cl 0.32 vs 0.55, transitorios de 18 conv. **Resuelta con advection_scheme="maccormack"** (coste ~4%, estacionario en t≈4-5 conv).
- Métrica decisiva: Q_lazo=∮u·n dl (runner la guarda como `q_loop_025c/075c`).

**Añadido sesiones previas**: monitor Kutta (`kutta_dcp_vector`), SDF polígono (`ibm_sdf_source="polygon"`), SA completo, Kutta explícita opcional (`kutta_enforce`), fix acumulación de presión MG compatible_flux (CG ya estaba bien), checkpoints.

**GA de optimización de perfiles (`scripts/RunGA.py`) — migrado a config fiable + estudio de convergencia ejecutado (commit `efdb799`, 2026-07-09)**: `simular_perfil` corre sobre consistent+SA+MacCormack (dx=0.002 por defecto, nu=1e-5), `main(config)` reutilizable con retorno dict, población mixta multi-semilla (`archivos_base` + `resamplear_a_grid` para cruce entre perfiles de distinto nº de puntos), parada por `tiempo_limite_s`. S1: mutación con sigma adaptativa (`sigma_factor_inicial/final`, exploración→refinamiento). Orquestador `scripts/agent_tests/run_convergence_study.py` (deadline-driven, resumible): Fase0 calibración dx (0.004 vs 0.002), Arm A GA por-semilla (diagnóstico), Arm B GA población mixta (arreglo), S2 islas con migración (`--islands`), S3 refine top-k a dx=0.002 (`--refine`, auto tras estudio), análisis automático → `results/convergence_study/`. Test roto arreglado (`tests/conftest.py` + sys.path). El GA integra memoria de ML persistente: `cerebro_aerodinamico.pkl` (histórico de experiencias) + `aprendizaje_ML.jsonl` (registros de simulación, sigue creciendo con cada sesión de GA/barridos).

**Resultado de la corrida de convergencia** (`RESUMEN_convergencia.md`, α=4°, Re=1e5, CFL=0.5): **Arm A NO converge** — 4 semillas (AG24, GM15, NACA_0012_sharp, s1014) caen en óptimos locales distintos, L/D spread=3.23, CV=0.172 (rango 6.71–9.95). Arm B (población mixta) da L/D=9.03, población final muy apretada (std=0.006) pero por debajo del mejor de Arm A — sugiere pocas generaciones dentro del deadline, no fitness plano. Distancia de forma media entre ganadores=0.0375 (formas distintas, no un solo óptimo). **T1 (ranking dx=0.004 vs dx=0.002, `results/convergence_study/tests/t1_ranking_dx.json`) confirma el sesgo de fitness sospechado**: a dx=0.004 gana AG24 (L/D=9.95), a dx=0.002 gana GM15 (L/D=14.46, vs AG24=12.90) — el ranking se invierte entre resoluciones, Δ(L/D) por refinar va de 1.9 a 5.15 según semilla (no uniforme, no es solo un offset). **Conclusión: dx=0.004 no es apto para seleccionar ganadores del GA; el refine S3 a dx=0.002 es obligatorio**, no opcional.

**Corrida extendida "reforzado" (`results/convergence_study/reforzado/`, sin commitear, 2026-07-09 23:01 → 2026-07-10, deadline=11h, duración real 737 min ≈ 12.3h)**: repite Arm A + Arm B con la calibración ya existente reutilizada (`calibration.json`: USE_DX=0.004, abort=False — nota: este archivo trae también un par `dx4_a5` L/D=6.98 vs `dx2_a5` L/D=8.76, brecha grande pese a `accept_dx004=true`; **pendiente reconciliar con la conclusión T1 de que dx=0.004 no es apto para rankear**, puede ser una config/semilla distinta a la del estudio original). Carga de memoria ML con error no fatal ("inhomogeneous shape" en `cerebro_aerodinamico.pkl`, se reinicia y sigue). **Arm B (mixed) mejora +54.0% (+3.25) sobre BASE, gana con L/D=9.27**, perfil guardado en `reforzado/armB/mixed/NACA_0012_sharp_Re100000_a4.0_LD9.27.dat` (con `archivo_base` listo para continuar el linaje). **S3 refine corrió sin presupuesto suficiente**: de 26 candidatos solo intentó refinar top-3, terminó con refinado parcial — el ganador de exploración (`islands/s1014/epoch3`) quedó sin refinar, `islands/refine_dx002.json` escrito vacío/parcial. **Reconfirma que S3 (refine a dx=0.002) es el cuello de botella de presupuesto del pipeline**, no un paso opcional.

**Fix de visualización en islas (commit `bf6824e`, 2026-07-09)**: en modo islas `archivo_base` es el ganador de la época previa (el padre), pero se etiquetaba como "Original" en `comparativa_perfiles.png`, confundiendo semilla genuina con padre intermedio. `visualizar_comparativa` ahora acepta `original_real`: dibuja la semilla genuina (verde) aparte de la base/padre (gris) cuando difieren. `CONFIG['archivo_original']` (opcional, cargado por `main`); `ejecutar_islas` fija `archivo_original` a la semilla real de cada isla.

**Reorganización de salidas de `results/convergence_study/` (commit `ac235b1`, 2026-07-09)**: la carpeta ahora separa `base_study/` (Fase0 + Arm A/B), `islands/` (S2 con migración) y `tests/`, en vez de mezclar todo en la raíz. Sin cambios de lógica en el orquestador, solo reordenamiento de paths de salida — cualquier script o notebook que referencie rutas antiguas de `results/convergence_study/*.json` directamente debe actualizarse a la subcarpeta correspondiente. La corrida "reforzado" añadió además `reforzado/`, `calibration.json` y `reforzado_run.log` en la raíz de `convergence_study/` (sin commitear, siguen sin commitear a fecha de esta actualización).

**S3 fix de sobrescritura (commit `9309f00`, 2026-07-10)**: `refinar_top_k` escribía `refine_dx002.json` con `ganador_refinado=None` cuando el deadline se agotaba antes de refinar ningún candidato, machacando un resultado válido de una corrida anterior. Ahora si no hay filas refinadas la función retorna sin escribir el archivo — protege corridas previas de S3 parcial contra el problema de presupuesto descrito arriba en "reforzado".

**Test aislado a convergencia añadido (commit `ea89a16`, 2026-07-10)**: para responder si perfiles distintos convergen al mismo óptimo SIN migración (a diferencia de Arm A, que corría a nº fijo de generaciones y podía cortar antes de converger), se añadió:
- `RunGA.py`: parada por estancamiento en el bucle evolutivo (corta si el mejor global no mejora > `tol_mejora_fitness` durante `paciencia_generaciones` consecutivas). Config nueva: `parada_estancamiento`, `paciencia_generaciones`, `tol_mejora_fitness`.
- `run_convergence_study.py`: modo `--aislado` (función `ejecutar_aislado`) — corre cada semilla sola (sin migración) hasta estancarse, pop y gen_max configurables vía `--aislado-pop` (12), `--aislado-gen-max` (30), `--aislado-paciencia` (6); sigma adaptativa. Salida en `aislado_sin_migracion_convergido/<semilla>/` + análisis de distancia-forma entre los 4 ganadores → `convergencia_aislado.json`.

**Aislado pausable/reanudable con checkpoint de genes (commit `30c7cf6`, 2026-07-10)**: la corrida `--aislado` es larga (hasta 40h por defecto) y no había forma de pausar sin perder progreso. Fix:
- `RunGA.py`: `estado_ga.json` ahora guarda los genes de toda la población (no solo el mejor global) + contadores de estancamiento; `main()` reanuda mid-corrida desde ese checkpoint si no existe `estado_ga_final.json` para la semilla. Una pausa por Ctrl+C no escribe `estado_ga_final.json`, así el relanzamiento continúa desde la última generación completada en vez de saltarla o reiniciar.
- `scripts/agent_tests/aislado.sh` (nuevo): lanzador en primer plano con progreso en vivo (tee a `results/convergence_study/aislado_run.log`), subcomando `status` que reporta por semilla (pendiente / en curso con gen y mejor L/D / convergida con L/D final) leyendo `estado_ga.json`/`estado_ga_final.json`, pausa con Ctrl+C, reanudación relanzando el mismo comando. `DEADLINE` configurable por variable de entorno (default 40h).

**Resultado del test aislado DEFINITIVO — ESTUDIO CERRADO (commit `a0ee4f3`, 2026-07-11 → 12, ~28h GPU, `convergencia_aislado.json`, pop12, sigma adaptativa, parada por estancamiento paciencia6)**: las 4 semillas corridas SOLAS hasta converger (7–25 gen cada una) **NO convergen a la misma forma** — dist-forma media=**0.0557** (igual/peor que la partida 0.0507), spread L/D=2.44. Cada semilla mejora su L/D pero acaba en geometría distinta: **AG24 L/D=11.95** (Cl0.84/Cd0.070, fino), NACA_0012_sharp 11.02 (Cl0.95/Cd0.086), GM15 10.91 (Cl0.76/Cd0.070, solo 7 gen), s1014 9.50 (Cl1.11/Cd0.117, gordo). **Conclusión (corrige la lectura previa de las islas): la convergencia que producen las islas la fuerza la MIGRACIÓN (formas comunes por flujo de genes), NO una física de óptimo único inevitable — bajo estas condiciones hay varias cuencas locales y no existe un óptimo global único alcanzable de forma independiente.** Cierra el matiz del "reforzado" (que solo hacía 3–6 gen). Ganadores `.dat` en `results/convergence_study/aislado_sin_migracion_convergido/<semilla>/<semilla>_Re100000_a4.0_LD*.dat`. Con este resultado el ciclo de estudio de convergencia (Arm A/B → islas → aislado) queda cerrado; próximos pasos son de dirección (más diversidad/multi-arranque) y de higiene de presupuesto (S3), no más corridas de diagnóstico de la misma pregunta.

**Working tree sin commitear a fecha de esta actualización (2026-07-12)**: además de la reorganización de `convergence_study/` (`reforzado/`, `calibration.json`, `reforzado_run.log`, `aislado_run.log`, `aislado_sin_migracion_convergido/`, `base_study/`, `islands/`, `tests/` — todos untracked), hay cambios modificados sin commitear en `Simulador2D.py` (rename `core_v0x/core_v0y/core_box`→`v0x_ic/v0y_ic`, ver arriba), `scripts/comparativa_barrido_completo.py` y `scripts/script_barrido_alpha.py` (separación Cl_final/Cl_cp + modo `--smoke`, ver arriba), y regeneración local de `results/comparativas/resultados_comparacion_outer_sum/*` con borrado de las carpetas antiguas `results/comparativas/resultados_comparacion/{AG24,GM15,NACA0012}_{100k,1M}`. `aprendizaje_ML.jsonl`, `cerebro_aerodinamico.pkl`, `polar_results.json` y `sim_last.npz` también aparecen modificados (subproductos normales de correr GA/barridos, no requieren revisión de diseño). Directorios untracked sin propósito documentado: `prueba_inicialesdiferentes/` (frames sueltos del bloque `__main__` de scratch con AG24, ver arriba) y `results/comparativas_v1/` (copia paralela de `resultados_comparacion` + `resultados_comparacion_outer_sum`, origen no documentado).

## Próximos pasos
1. **Estudio de convergencia CERRADO (2026-07-12)**: Arm A/B → islas → aislado agotaron la pregunta "¿converge el GA a un óptimo único?" — respuesta: no, hay varias cuencas locales y la migración es el mecanismo de consenso, no física de óptimo global. No relanzar más corridas de diagnóstico de esta misma pregunta. Direcciones abiertas: si se quiere un óptimo global real, GA con más diversidad/multi-arranque o aceptar migración como mecanismo de consenso; y revisar físicamente el óptimo de camber alto (Cl~1.07 a α=4, semilla s1014).
2. **Reconciliar `calibration.json` de la corrida "reforzado" con la conclusión T1**: `accept_dx004=true` con brecha L/D dx4 vs dx2 de ~1.8 en a5 (6.98 vs 8.76) contradice en apariencia "dx=0.004 no apto para rankear" — revisar si es una calibración de otra config/semilla antes de confiar en el ganador L/D=9.27 de Arm B sin refinar a dx=0.002.
3. **Decidir presupuesto del GA a la luz de T1**: si el ranking cambia de orden entre dx=0.004 y dx=0.002, evaluar población completa a dx=0.002 desde el inicio (más caro, ~460s/individuo vs ~100-115s) o usar dx=0.004 solo como filtro grosero (top-N) seguido de refine S3 en todos los N, no solo el top-1.
4. Repetir el estudio de convergencia con más generaciones/deadline más largo para el Arm B (mixed) y ver si supera a Arm A con más tiempo, o si el gap L/D=9.03 vs 9.95 es real (indicaría que population mixing sacrifica rendimiento por diversidad). La corrida "reforzado" ya apunta a que sí mejora con más presupuesto (L/D=9.27), pero sin refine S3 completo el número no es comparable 1:1 con los de dx=0.002 del estudio original.
5. Dar presupuesto explícito y suficiente a S3 (refine top-k a dx=0.002) en las próximas corridas — en "reforzado" quedó sin ejecutar sobre el ganador real por falta de tiempo, dos veces seguidas ya (estudio original y reforzado). El fix `9309f00` ya evita que un S3 parcial borre un resultado válido previo, pero no resuelve la falta de presupuesto en sí.
6. **Ejecutar el barrido** `scripts/agent_tests/run_cfl_sweep.py` (30 sims, resumible) para caracterizar coste/error CFL×dx×α — sigue pendiente, ninguna sim lanzada aún.
7. Investigar el error no fatal de carga de `cerebro_aerodinamico.pkl` ("inhomogeneous shape") visto en el log de "reforzado" — no bloquea (se reinicia la memoria), pero pierde el histórico de experiencias previas si se repite.
8. Opcional barato: probar MacCormack+turbo_hd (~19 it/s esperado) para pre-screening si el sesgo por Q=0.026 resulta consistente entre geometrías.
9. Opcional física: sublinealidad α=8 (deriva -0.002, TE separación creciendo) — vigilar si el optimizador explora α altos.
10. **Limpieza pendiente en el árbol de trabajo** (ver detalle en "Working tree sin commitear" arriba): decidir y commitear la reorganización de `results/convergence_study/` (incluida "reforzado"); confirmar si el borrado de `results/comparativas/resultados_comparacion/{AG24,GM15,NACA0012}_{100k,1M}` es intencional y commitearlo junto con la regeneración de `resultados_comparacion_outer_sum/`; commitear o descartar el rename `core_v0x`→`v0x_ic` en `Simulador2D.py` y los cambios de `--smoke`/Cl_cp en los scripts de barrido; decidir destino de `prueba_inicialesdiferentes/` y `results/comparativas_v1/` (conservar, fusionar o borrar como scratch).

## Tests
- `scripts/agent_tests/run_kutta_tests.py {smoke|smoke_polygon|baseline_a5|polygon_a5|compare|regression}` — runs coarse (dx=0.004, ~5-15 min) con criterios cuantitativos, resultados en `results/agent_tests/`.
- `scripts/agent_tests/run_cfl_sweep.py` — barrido CFL/dx/α resumible (ver Próximos pasos), resultados en `results/cfl_sweep/`.
- `scripts/agent_tests/run_convergence_study.py --aislado` — test de convergencia por semilla sin migración, parada por estancamiento, pausable/reanudable vía checkpoint de genes (ver Estado actual). Lanzador recomendado: `scripts/agent_tests/aislado.sh` (foreground, progreso en vivo, `aislado.sh status` para consultar avance). Resultados en `results/convergence_study/aislado_sin_migracion_convergido/`. **Estudio cerrado** (ver Próximos pasos #1); no relanzar salvo para explorar direcciones nuevas (más diversidad/multi-arranque).
- `scripts/comparativa_barrido_completo.py --smoke` — validación rápida del pipeline outer_sum vs XFoil (1 perfil, 1 Re, 3 alphas, malla gruesa, 50 iters) sin gastar GPU real, salida en `results/comparativas/smoke_test/`.
- `pytest tests/ --ignore=tests/test_generacion_geometrica_ga.py` (5 passed; el de GA está roto pre-existente: importa `RunGA` inexistente).

## Diagnósticos clave (métodos de Mesh)
- `compute_circulation()` — Γ en lazos, Cl_circ=-2Γ/(U·c).
- `compute_cp_diagnostics(mu, rho)` — Cl(∮ΔCp), ΔCp_TE, pico de succión.
- `compute_kutta_dcp_instant(face_data)` — ΔCp_TE instantáneo para el monitor.
- `debug_te_report()` — ghosts/irreparables cerca del TE.
- `plot_streamlines()`, `plot_forces_over_time()`, `plot_cp_vs_chord()`.

## Config de referencia (fina, `__main__`)
Lx=12, Ly=8, dx_min=0.001 (malla 3243×1203, ~2 it/s), CFL=0.25, NACA_0012_sharp, min_te_height_factor=1.0, turbo_hd, wake long_fine_x. **Nota**: el bloque `__main__` actual en el working tree (sin commitear) está configurado como scratch de prueba de transitorios (perfil AG24, `v0x_ic/v0y_ic`, `frames_dir_grueso="prueba_inicialesdiferentes"`, `stop_on_clcd_convergence=False`, 10k iters) y no refleja esta config de referencia — usarla como plantilla, no copiarla literal.
